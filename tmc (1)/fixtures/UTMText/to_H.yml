formalism: nesting minimising
start state: S
# to H (15 states = 1 + 7 + 9 - 2). Source: UTM.pdf p.6 bot.
# Chains: shift dub R subroutine, then verify γ' placed before H;
# if verify says no, loop back to shift dub R; if yes, done.
#
# resolved: the transit label from `shift dub R` exit to `verify γ'
# placed before H` reads as `α/α,R` in my zoom; could be `σ/σ,R`.
# NOTE FROM SAMHAIN: it *is* a sigma
table:
  S:
    - "shift dub R":
        sigma: [verify wrap, R]       # α/α,R — resolved unsure α vs σ
  verify wrap:
    - "verify gp placed before H":
        no epsilon: S              # loop: shift again
        yes epsilon: done
  done:

# WEPS edits here: changed line 14 to be in accordance with naming conventions of the repo.
# SRA changed the pseudo start to be named S.  I really don't like the need of
# `verify wrap`, but I don't feel like figuring out how to make it better.
